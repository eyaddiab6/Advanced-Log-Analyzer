# Advanced Log Analyzer

Advanced Log Analyzer is a Python-based cybersecurity project that analyzes Linux SSH authentication logs, detects suspicious activity, maps detections to MITRE ATT&CK techniques, and displays the results in an interactive Flask dashboard.

The project is designed as a lightweight SIEM-style security monitoring tool for learning log analysis, detection engineering, alert aggregation, and security visualization.

---

## Features

- Linux SSH authentication log parsing
- Config-driven detection rules
- Brute-force attack detection
- Successful login after multiple failures detection
- Root login attempt detection
- Off-hours login detection
- Password spraying detection
- Targeted account attack detection
- Trusted IP severity reduction
- Alert aggregation with configurable time windows
- Alert severity classification
- MITRE ATT&CK mapping
- Unique alert and occurrence tracking
- JSON security report export
- CSV alert export
- Interactive Flask dashboard
- Security charts using Chart.js
- Upload and analyze `.log` or `.txt` files
- Alert investigation panel
- Alert search
- Severity filtering
- Top attacking IP analysis
- Most targeted user analysis

---

## Detection Rules

### 1. Brute Force Attack

Detects multiple failed authentication attempts against the same username from the same IP address within a configured time window.

**MITRE ATT&CK**

- Technique: T1110.001
- Name: Password Guessing

---

### 2. Successful Login After Multiple Failures

Detects a successful login that occurs after several failed authentication attempts from the same source IP against the same account.

This can indicate that an attacker successfully guessed or obtained valid credentials.

**MITRE ATT&CK**

- Technique: T1078
- Name: Valid Accounts

---

### 3. Root Login Attempt

Detects authentication attempts targeting the privileged `root` account.

Successful root authentication attempts receive a higher severity level.

**MITRE ATT&CK**

- Technique: T1078
- Name: Valid Accounts

---

### 4. Off-Hours Login

Detects successful authentication events during configured off-hours.

The default configuration monitors logins between:

```text
00:00 - 05:00
```

**MITRE ATT&CK**

- Technique: T1078
- Name: Valid Accounts

---

### 5. Password Spraying Attack

Detects one source IP attempting authentication against several different usernames within a short time period.

**MITRE ATT&CK**

- Technique: T1110.003
- Name: Password Spraying

---

### 6. Targeted Account Attack

Detects one username being targeted by failed authentication attempts from several different source IP addresses.

**MITRE ATT&CK**

- Technique: T1110
- Name: Brute Force

---

## Alert Aggregation

Repeated alerts with the same:

- Alert type
- Source IP
- Username

can be grouped into one alert when they occur within the configured aggregation time window.

Example:

```text
13:01:00 Root Login Attempt
13:01:20 Root Login Attempt
```

can become:

```text
Occurrences: 2
First Seen: 2026-08-15 13:01:00
Last Seen: 2026-08-15 13:01:20
```

This reduces duplicate alerts and makes the output easier to investigate.

---

## Trusted IP Handling

Trusted IP addresses can be configured inside `config.json`.

Instead of suppressing alerts completely, the project reduces the severity of alerts generated by trusted IP addresses.

Example:

```text
CRITICAL -> HIGH
HIGH     -> MEDIUM
MEDIUM   -> LOW
LOW      -> LOW
```

The alert also keeps its original severity for investigation.

---

## Dashboard

The Flask dashboard provides a SOC-style interface for reviewing the analysis results.

The dashboard includes:

- Total parsed events
- Unique alerts
- Total alert occurrences
- Critical alerts
- High alerts
- Medium alerts
- Alerts by severity chart
- Login activity chart
- Top attacking IPs chart
- Most targeted users
- MITRE ATT&CK summary
- Security alerts table
- Search and severity filters
- Alert investigation view
- Log file upload
- Generated report information

---

## Alert Investigation

Clicking an alert in the Security Alerts table opens a detailed investigation section.

The investigation panel displays information such as:

- Alert ID
- Alert type
- Severity
- Source IP
- Username
- MITRE technique
- Occurrences
- Status
- Failed attempts
- Previous failures
- Time window
- First seen
- Last seen
- Detection explanation

---

## Log Upload

The dashboard supports uploading:

```text
.log
.txt
```

files directly from the browser.

After selecting a file and clicking:

```text
Analyze Logs
```

the application automatically performs:

```text
Upload
   ↓
Parse
   ↓
Detect
   ↓
Aggregate
   ↓
MITRE Mapping
   ↓
Generate Summary
   ↓
Export Reports
   ↓
Display Dashboard
```

---

## Example Log Format

The current version supports Linux SSH authentication logs similar to:

```text
Aug 15 12:10:31 server sshd[1234]: Failed password for admin from 192.168.1.20 port 55231 ssh2
Aug 15 12:10:35 server sshd[1235]: Failed password for admin from 192.168.1.20 port 55232 ssh2
Aug 15 12:10:38 server sshd[1236]: Failed password for admin from 192.168.1.20 port 55233 ssh2
Aug 15 12:10:50 server sshd[1238]: Accepted password for admin from 192.168.1.20 port 55235 ssh2
```

Sample logs are available in the `logs/` directory.

---

## Project Structure

```text
Advanced-Log-Analyzer/
│
├── logs/
│   ├── auth.log
│   └── test_attack.log
│
├── reports/
│
├── static/
│   └── style.css
│
├── templates/
│   └── dashboard.html
│
├── app.py
├── config.json
├── detector.py
├── main.py
├── parser.py
├── reporter.py
├── .gitignore
└── README.md
```

---

## Configuration

Detection rules are controlled through `config.json`.

Example:

```json
{
    "brute_force": {
        "enabled": true,
        "threshold": 3,
        "time_window": 60
    },

    "success_after_failures": {
        "enabled": true,
        "threshold": 3,
        "time_window": 120
    },

    "password_spraying": {
        "enabled": true,
        "user_threshold": 3,
        "time_window": 120
    },

    "targeted_account": {
        "enabled": true,
        "ip_threshold": 3,
        "time_window": 300
    },

    "off_hours": {
        "enabled": true,
        "start_hour": 0,
        "end_hour": 5
    },

    "root_login": {
        "enabled": true
    },

    "trusted_ips": [
        "192.168.1.20"
    ],

    "aggregation": {
        "time_window": 600
    }
}
```

Rules can be enabled or disabled without modifying the detection code.

---

## Installation

Clone the repository:

```bash
git clone https://github.com/eyaddiab6/Advanced-Log-Analyzer.git
```

Enter the project directory:

```bash
cd Advanced-Log-Analyzer
```

Install Flask:

```bash
pip install flask
```

---

## Running the Command-Line Analyzer

Run:

```bash
python main.py
```

The program will:

- Parse the default SSH log
- Execute the detection rules
- Display security alerts
- Generate a security summary
- Export JSON and CSV reports

---

## Running the Dashboard

Run:

```bash
python app.py
```

Then open:

```text
http://127.0.0.1:5000
```

in your browser.

---

## Generated Reports

The analyzer can generate:

```text
reports/security_report.json
reports/alerts.csv
```

These files are automatically generated during analysis and are excluded from Git tracking through `.gitignore`.

---

## Technologies Used

- Python
- Flask
- HTML
- CSS
- JavaScript
- Chart.js
- Regular Expressions
- JSON
- CSV
- MITRE ATT&CK

---

## Current Version

The current version focuses on:

```text
Linux SSH Authentication Logs
```

Future versions are planned to support additional log sources.

---

## Planned Improvements

Future development may include:

- Apache access log support
- Nginx access log support
- Automatic log source detection
- Web path scanning detection
- Sensitive file access detection
- High-volume request detection
- HTTP status code analysis
- Additional MITRE ATT&CK mappings
- Downloadable reports directly from the dashboard
- More advanced risk scoring
- Additional detection rules

The long-term goal is to expand the project into a multi-source lightweight SIEM-style log analysis platform.

---

## Disclaimer

This project was created for cybersecurity learning, defensive security research, and portfolio purposes.

It is not intended to replace a production SIEM or enterprise security monitoring platform.

---

## Author

**Eyad Diab**

Cybersecurity Student / Developer

GitHub:

```text
https://github.com/eyaddiab6
```
